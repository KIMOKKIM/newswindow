# 로그인 실패 원인 분류 (확정·해결)

## 관측된 증상 타임라인

| 단계 | 증상 | 직접 원인 분류 |
|------|------|----------------|
| A | 브라우저/운영에서 로그인 실패, 네트워크 404 | **프론트 요청 URL 오류**: `POST /api/api/auth/login` (`API`=`/api` 일 때 `/api` 중복) |
| B | 올바른 URL인데 401 | **비밀번호 해시 불일치** 또는 **계정 없음** (백엔드 도달 후) |
| C | 올바른 URL인데 500 `proxy_error` / `fetch failed` | **프록시 → 백엔드 HTTP fetch 실패** (백엔드 다운, 슬립, URL, TLS 등) — 인증 로직 전 단계 |

## 계정별 (수정 후 기준)

| 계정 | 실패했던 주원인 | 수정 후 |
|------|-----------------|--------|
| teomok1 | A(404) 가능성 큼 + B(해시) | URL 수정 + `users.json` 해시 재설정 |
| teomok2 | 동일 | 동일 |
| admin1 | 동일 | 동일 |

아래는 **해당 없음**으로 확정:

- role 불일치 (JSON 상 role 과 login.html 분기 일치)
- dev bypass (사용 안 함)
- 로그인 성공 후 세션 저장 실패 (localStorage 실패는 브라우저 환경 의존, 서버 200 시 별도)
